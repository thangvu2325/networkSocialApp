import { FunctionComponent } from "react";
import { useInView } from "react-intersection-observer";
interface LoadingMorePostProps {}

const LoadingMorePost: FunctionComponent<LoadingMorePostProps> = () => {
  return <p></p>;
};

export default LoadingMorePost;
